import React from 'react';
// import PropTypes from 'prop-types';

const ShowInfoOrder = (props) => {
  return <section className="show-info-order-wrapper">{props}</section>;
};

// ShowInfoOrder.propTypes = {
// };

export default ShowInfoOrder;
